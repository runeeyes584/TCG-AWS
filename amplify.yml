version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: Front-end/out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - Front-end/.next/cache/**/*